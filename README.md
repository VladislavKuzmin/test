# Forks Sync Manager

Автоматическая синхронизация всех форков с оригинальными репозиториями.

## 📋 Описание

Этот репозиторий содержит скрипты и рабочие процессы GitHub Actions для автоматической синхронизации форков из аккаунта `HostmanAppsStage` с оригинальными репозиториями из `timeweb-cloud`.

## 🕐 Расписание

Синхронизация запускается **каждый день в 03:00 UTC**.

## 🚀 Как это работает

1. **GitHub Actions** запускает скрипт по расписанию
2. Скрипт перебирает все репозитории из списка
3. Для каждого репозитория:
    - Клонирует форк
    - Добавляет/обновляет upstream remote
    - Получает изменения из оригинала
    - Выполняет синхронизацию (fast-forward или merge)
    - Пушит изменения обратно в форк
4. Создает отчет о результатах

## ⚙️ Настройка

### 1. Создайте Personal Access Token

1. Перейдите в [GitHub Settings → Developer settings → Personal access tokens](https://github.com/settings/tokens)
2. Создайте новый **classic token**
3. Выберите права:
    - ✅ `repo` (Full control of private repositories)
    - ✅ `workflow` (Update GitHub Action workflows)
4. Установите **Expiration: No expiration**
5. Скопируйте токен

### 2. Добавьте секрет в этот репозиторий

1. Откройте **Settings** → **Secrets and variables** → **Actions**
2. Нажмите **"New repository secret"**
3. Name: `SYNC_TOKEN`
4. Value: ваш токен
5. Нажмите **Add secret**

### 3. Запустите вручную (опционально)

Перейдите в **Actions** → **Sync All Forks with Upstream** → **Run workflow**

## 📊 Мониторинг

После каждого запуска:
- Просмотрите логи в разделе **Actions**
- Скачайте артефакт `sync-summary` для детального отчета
- Проверьте статус выполнения

## 📝 Список синхронизируемых репозиториев

См. файл `repos.txt` или переменную `REPOS` в `scripts/sync-forks.sh`.

## 🔧 Обновление списка репозиториев

Отредактируйте массив `REPOS` в файле `scripts/sync-forks.sh` и закоммитьте изменения.

## 📧 Уведомления

Настройте уведомления о неудачных запусках:
1. Перейдите в **Settings** → **Notifications**
2. Включите уведомления для этого репозитория